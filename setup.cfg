[aliases]
test=pytest

[tool:pytest]
testpaths = pydeep_sim/tests/integration_tests

#addopts = --doctest-modules --doctest-continue-on-failure --doctest-ignore-import-errors --cov-report=term --cov
#if you want to see ouput always, set --capture=no or -s
# disable warnings --disable-warnings
addopts = --no-cov

[coverage:run]
source = pydeep_sim


